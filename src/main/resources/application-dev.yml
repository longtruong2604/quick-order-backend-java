server:
  port: 8081
spring:
  config:
    activate:
      on-profile: dev
openapi:
  service:
    api-docs: api-service
    title: Quick Order API
    version: 1.0.0
    server: http://localhost:{server.port}
